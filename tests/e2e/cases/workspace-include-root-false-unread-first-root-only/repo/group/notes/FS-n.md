# FS-n: N

Nothing scans this file. `notes` is the block's *first* scan root in config
order while `docs` is the first alphabetically, so the root the warning names
says which order fixes the choice.
