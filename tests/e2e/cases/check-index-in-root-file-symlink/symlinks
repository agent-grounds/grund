# The target is inside the config root but outside the direct scan root, so the
# index has one unambiguous scanned spelling and remains writable by `fmt`.
project/docs/fs/README.md -> ../../indexes/fs.md
