# Guide

The member's rule is §api/FS-x, and its qualified citation shape is written
`<§>api/FS-x` — the alias and the ID both resolve, so the escape is either an
illustration or a marker bracketed by accident, and grund names the live form
to switch to.
