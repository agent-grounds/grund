grund_config_version = 1
project_name = "root"

[reference]
marker = "§"
strict = true

[id]
format = "{kind}-{slug}"
slug_pattern = "[a-z][a-z0-9-]*"

[scan]
include = ["docs"]

[fmt.cross_refs]
anchor_format = "github"

[workspace]
members = ["packages/api"]
