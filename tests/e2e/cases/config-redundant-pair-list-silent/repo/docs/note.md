# Note

Mentions FS-001-nowhere without a marker. Only the bare `grund.toml` is read,
and it sets `strict = true`; the ignored `.agents/grund.toml` beside it sets
`strict = false`. A redundant pair is a fact about the repository's
configuration, so the config surfaces are where the run says so.
