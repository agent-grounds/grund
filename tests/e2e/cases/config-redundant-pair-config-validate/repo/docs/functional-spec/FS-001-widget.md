# FS-001-widget: A widget does one thing

The widget does it, and it is declared here so the run has something to
answer about — the case is about which config file was read, not about what
the walk found.
